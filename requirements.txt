# Legal Clause Simplifier — Python dependencies
# Install with:  pip install -r requirements.txt

# PDF extraction
PyPDF2>=3.0.0

# NLP / clause splitting
spacy>=3.7.0
# After installing, download the spaCy model:
#   python -m spacy download en_core_web_sm

# Groq SDK (free cloud LLM API — replaces all local BART/PEGASUS models)
python-dotenv>=1.0.0
groq>=0.9.0

# Semantic similarity (local, lightweight, CPU-friendly)
sentence-transformers>=2.7.0
torch>=2.2.0

# Evaluation metrics
nltk>=3.8.1
rouge-score>=0.1.2
scikit-learn>=1.4.0
numpy>=1.26.0